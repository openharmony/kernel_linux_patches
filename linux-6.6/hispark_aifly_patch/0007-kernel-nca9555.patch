diff --git a/arch/arm64/boot/dts/vendor/ss928v100-demb.dts b/arch/arm64/boot/dts/vendor/ss928v100-demb.dts
index d90c5d5cf..b488894f5 100644
--- a/arch/arm64/boot/dts/vendor/ss928v100-demb.dts
+++ b/arch/arm64/boot/dts/vendor/ss928v100-demb.dts
@@ -157,6 +157,11 @@ &i2c_bus2 {
 
 &i2c_bus3 {
 	status = "okay";
+	nca9555: ioexpander@20 {
+        compatible = "nca9555";
+        reg = <0x20>;
+        status = "okay";
+    };
 };
 
 &i2c_bus4 {
diff --git a/drivers/gpio/Kconfig b/drivers/gpio/Kconfig
index ebd4e113d..7fbce8891 100644
--- a/drivers/gpio/Kconfig
+++ b/drivers/gpio/Kconfig
@@ -1821,3 +1821,7 @@ config GPIO_SIM
 endmenu
 
 endif
+
+config GPIO_NCA9555
+	tristate "NCA9555"
+	default n
diff --git a/drivers/gpio/Makefile b/drivers/gpio/Makefile
index f1f998da4..69407b9c5 100644
--- a/drivers/gpio/Makefile
+++ b/drivers/gpio/Makefile
@@ -196,3 +196,4 @@ obj-$(CONFIG_GPIO_ZEVIO)		+= gpio-zevio.o
 obj-$(CONFIG_GPIO_ZYNQ)			+= gpio-zynq.o
 obj-$(CONFIG_GPIO_ZYNQMP_MODEPIN)	+= gpio-zynqmp-modepin.o
 obj-$(CONFIG_ARCH_HISILICON)			+= vendor/
+obj-$(CONFIG_GPIO_NCA9555)			+= gpio-nca9555.o
\ No newline at end of file
diff --git a/drivers/gpio/gpio-nca9555.c b/drivers/gpio/gpio-nca9555.c
new file mode 100644
index 000000000..075c580da
--- /dev/null
+++ b/drivers/gpio/gpio-nca9555.c
@@ -0,0 +1,207 @@
+// SPDX-License-Identifier: GPL-2.0-or-later
+/*
+ * GPIO driver for NCA9555 MFD
+ *
+ */
+
+#include <linux/module.h>
+#include <linux/slab.h>
+#include <linux/kernel.h>
+#include <linux/init.h>
+#include <linux/platform_device.h>
+#include <linux/mfd/nca9555.h>
+#include <linux/gpio/driver.h>
+
+#define NCA9555_GPIO_MAX 16
+
+struct nca9555_gpio_dev {
+	struct device *parent;
+	struct gpio_chip gpio_chip;
+	struct mutex lock;
+	u8 dat_out[2];
+	u8 dir[2];
+};
+
+static int nca9555_gpio_reg_read(struct nca9555_gpio_dev *nca9555_gpio, u8 reg, u8 *val)
+{
+	struct nca9555_dev *nca9555;
+	nca9555 = dev_get_drvdata(nca9555_gpio->parent);
+
+	return nca9555->read_reg(nca9555, reg, val);
+}
+
+static int nca9555_gpio_reg_write(struct nca9555_gpio_dev *nca9555_gpio, u8 reg, u8 val)
+{
+	struct nca9555_dev *nca9555;
+	nca9555 = dev_get_drvdata(nca9555_gpio->parent);
+
+	return nca9555->write_reg(nca9555, reg, val);
+}
+
+static int nca9555_gpio_get_value(struct gpio_chip *chip, unsigned int off)
+{
+	struct nca9555_gpio_dev *nca9555_gpio;
+	unsigned int bank, bit;
+	u8 val;
+
+	nca9555_gpio = gpiochip_get_data(chip);
+	bank = NCA9555_BANK(off);
+	bit = NCA9555_BIT(off);
+
+	mutex_lock(&nca9555_gpio->lock);
+	/* There are dedicated registers for GPIO IN/OUT. */
+	if (nca9555_gpio->dir[bank] & bit)
+		val = nca9555_gpio->dat_out[bank];
+	else
+		nca9555_gpio_reg_read(nca9555_gpio, GPIO_PORT0_INPUT_REG + bank, &val);
+
+	mutex_unlock(&nca9555_gpio->lock);
+
+	return !!(val & bit);
+}
+
+static void nca9555_gpio_set_value(struct gpio_chip *chip, unsigned int off, int val)
+{
+	struct nca9555_gpio_dev *nca9555_gpio;
+	unsigned int bank, bit;
+
+	nca9555_gpio = gpiochip_get_data(chip);
+	bank = NCA9555_BANK(off);
+	bit = NCA9555_BIT(off);
+
+	mutex_lock(&nca9555_gpio->lock);
+	if (val)
+		nca9555_gpio->dat_out[bank] |= bit;
+	else
+		nca9555_gpio->dat_out[bank] &= ~bit;
+
+	nca9555_gpio_reg_write(nca9555_gpio, GPIO_PORT0_OUTPUT_REG + bank,
+			       nca9555_gpio->dat_out[bank]);
+	mutex_unlock(&nca9555_gpio->lock);
+}
+
+static int nca9555_gpio_direction_input(struct gpio_chip *chip, unsigned int off)
+{
+	struct nca9555_gpio_dev *nca9555_gpio;
+	unsigned int bank, bit;
+	int ret;
+
+	nca9555_gpio = gpiochip_get_data(chip);
+	bank = NCA9555_BANK(off);
+	bit = NCA9555_BIT(off);
+
+	mutex_lock(&nca9555_gpio->lock);
+	nca9555_gpio->dir[bank] |= bit;
+	ret = nca9555_gpio_reg_write(nca9555_gpio, GPIO_PORT0_CONFIG_REG + bank,
+				     nca9555_gpio->dir[bank]);
+	mutex_unlock(&nca9555_gpio->lock);
+	return ret;
+}
+
+static int nca9555_gpio_direction_output(struct gpio_chip *chip, unsigned int off, int val)
+{
+	struct nca9555_gpio_dev *nca9555_gpio;
+	unsigned int bank, bit;
+	int ret;
+
+	nca9555_gpio = gpiochip_get_data(chip);
+
+	bank = NCA9555_BANK(off);
+	bit = NCA9555_BIT(off);
+
+	mutex_lock(&nca9555_gpio->lock);
+	// 配置为输出：先更新输出电平寄存器，再设置方向寄存器
+	// 设计目的：防止先切输出方向时引脚出现未知电平毛刺
+	nca9555_gpio->dir[bank] &= ~bit;
+
+	if (val)
+		nca9555_gpio->dat_out[bank] |= bit;
+	else
+		nca9555_gpio->dat_out[bank] &= ~bit;
+
+	// 1. 先写入目标输出电平
+	ret = nca9555_gpio_reg_write(nca9555_gpio, GPIO_PORT0_OUTPUT_REG + bank,
+				     nca9555_gpio->dat_out[bank]);
+	// 2. 再配置引脚为输出模式
+	ret |= nca9555_gpio_reg_write(nca9555_gpio, GPIO_PORT0_CONFIG_REG + bank,
+				      nca9555_gpio->dir[bank]);
+
+	mutex_unlock(&nca9555_gpio->lock);
+
+	return ret;
+}
+
+static int nca9555_gpio_probe(struct platform_device *pdev)
+{
+	struct nca9555_gpio_dev *nca9555_gpio;
+	struct device *dev = &pdev->dev;
+	struct device_node *np = dev->of_node;
+	struct gpio_chip *gc;
+	int i;
+	int ret;
+
+	dev_info(dev, "gpio-nca9555 probe start \n");
+
+	nca9555_gpio = devm_kzalloc(&pdev->dev, sizeof(struct nca9555_gpio_dev), GFP_KERNEL);
+	if (!nca9555_gpio)
+		return -ENOMEM;
+
+	nca9555_gpio->parent = pdev->dev.parent;
+
+	gc = &nca9555_gpio->gpio_chip;
+	gc->fwnode = dev_fwnode(dev);
+	gc->parent = dev;
+	gc->direction_input  = nca9555_gpio_direction_input;
+	gc->direction_output = nca9555_gpio_direction_output;
+	gc->get = nca9555_gpio_get_value;
+	gc->set = nca9555_gpio_set_value;
+	gc->can_sleep = true;
+
+	gc->base = -1;
+	gc->ngpio = NCA9555_GPIO_MAX;
+	gc->label = pdev->name;
+	gc->owner = THIS_MODULE;
+
+	mutex_init(&nca9555_gpio->lock);
+
+	for (i = 0; i < 2; i++) {
+		u8 *dat_out, *dir;
+		dat_out = nca9555_gpio->dat_out;
+		dir = nca9555_gpio->dir;
+		ret = nca9555_gpio_reg_read(nca9555_gpio,
+				      GPIO_PORT0_OUTPUT_REG + i, dat_out + i);
+		if (ret) {
+			dev_err(dev, "read output port%d reg fail, ret=%d\n", i, ret);
+			return ret;
+		}
+		ret = nca9555_gpio_reg_read(nca9555_gpio,
+				      GPIO_PORT0_CONFIG_REG + i, dir + i);
+		if (ret) {
+			dev_err(dev, "read config port%d reg fail, ret=%d\n", i, ret);
+			return ret;
+		}
+	}
+	
+	dev_info(dev, "gpio-nca9555 probe succes \n");
+
+	return devm_gpiochip_add_data(&pdev->dev, &nca9555_gpio->gpio_chip, nca9555_gpio);
+}
+
+static const struct of_device_id nca9555_of_match[] = {
+	{.compatible = "nca9555-gpio", },
+	{ /* sentinel */ }
+};
+MODULE_DEVICE_TABLE(of, nca9555_of_match);
+
+static struct platform_driver nca9555_gpio_driver = {
+	.driver	= {
+		.name	= "nca9555-gpio",
+		.of_match_table = nca9555_of_match,
+	},
+	.probe		= nca9555_gpio_probe,
+};
+
+module_platform_driver(nca9555_gpio_driver);
+
+MODULE_DESCRIPTION("GPIO NCA9555 Driver");
+MODULE_LICENSE("GPL");
diff --git a/drivers/mfd/Kconfig b/drivers/mfd/Kconfig
index 459e737c5..3f6687e8f 100644
--- a/drivers/mfd/Kconfig
+++ b/drivers/mfd/Kconfig
@@ -2345,3 +2345,8 @@ config MFD_RSMU_SPI
 
 endmenu
 endif
+
+config MFD_NCA9555
+	tristate "NCA9555"
+	select MFD_CORE
+	default n
diff --git a/drivers/mfd/Makefile b/drivers/mfd/Makefile
index 7e57a4983..f1ac14598 100644
--- a/drivers/mfd/Makefile
+++ b/drivers/mfd/Makefile
@@ -285,3 +285,4 @@ obj-$(CONFIG_MFD_ATC260X_I2C)	+= atc260x-i2c.o
 
 obj-$(CONFIG_MFD_RSMU_I2C)	+= rsmu_i2c.o rsmu_core.o
 obj-$(CONFIG_MFD_RSMU_SPI)	+= rsmu_spi.o rsmu_core.o
+obj-$(CONFIG_MFD_NCA9555)	+= nca9555.o
diff --git a/drivers/mfd/nca9555.c b/drivers/mfd/nca9555.c
new file mode 100644
index 000000000..30f6de079
--- /dev/null
+++ b/drivers/mfd/nca9555.c
@@ -0,0 +1,158 @@
+/* SPDX-License-Identifier: GPL-2.0-or-later */
+/* NCA9555 IO Expander, Key controller core driver.
+ *
+ * Copyright 2023 RPDZKJ
+ */
+
+#include <linux/module.h>
+#include <linux/moduleparam.h>
+#include <linux/init.h>
+#include <linux/slab.h>
+#include <linux/i2c.h>
+#include <linux/of.h>
+#include <linux/mfd/core.h>
+#include <linux/mfd/nca9555.h>
+#include <linux/io.h>
+static const struct mfd_cell nca9555_devs[] = {
+	{
+		.name = "nca9555-gpio",
+		.of_compatible = "nca9555-gpio",
+	}
+};
+
+static int nca9555_i2c_read_reg(struct nca9555_dev *nca9555, u8 reg, u8 *val)
+{
+	struct i2c_client *i2c = nca9555->i2c_client;
+	struct i2c_msg xfer[2];
+	int ret;
+
+	/* Write register */
+	xfer[0].addr = i2c->addr;
+	xfer[0].flags = 0;
+	xfer[0].len = 1;
+	xfer[0].buf = &reg;
+
+	/* Read data */
+	xfer[1].addr = i2c->addr;
+	xfer[1].flags = I2C_M_RD;
+	xfer[1].len = 1;
+	xfer[1].buf = val;
+
+	ret = i2c_transfer(i2c->adapter, xfer, 2);
+	if (ret == 2) {
+		return 0;
+	} else {
+		dev_err(&i2c->dev,
+			"Failed to read reg 0x%02x, ret is %d\n",
+			reg, ret);
+		if ( ret >=0)
+			return -EIO;
+		else
+			return ret;
+	}
+}
+
+static int nca9555_i2c_write_reg(struct nca9555_dev *nca9555, u8 reg, u8 val)
+{
+	struct i2c_client *i2c = nca9555->i2c_client;
+	u8 msg[2];
+	int ret;
+
+	msg[0] = reg;
+	msg[1] = val;
+	ret = i2c_master_send(i2c, msg, 2);
+	if (ret == 2) {
+		return 0;
+	} else {
+		dev_err(&i2c->dev,
+			"Failed to write reg 0x%02x, ret is %d\n",
+			reg, ret);
+		if ( ret >=0)
+			return -EIO;
+		else
+			return ret;
+	}
+}
+
+static int nca9555_i2c_probe(struct i2c_client *i2c)
+{
+	struct nca9555_dev *nca9555;
+	int ret;
+	struct device *dev = &i2c->dev;
+
+	dev_info(dev, "Enter probe, i2c addr:0x%02x\n", i2c->addr);
+
+	nca9555 = devm_kzalloc(dev, sizeof(struct nca9555_dev), GFP_KERNEL);
+	if (!nca9555) {
+		dev_err(dev, "devm_kzalloc failed\n");
+		return -ENOMEM;
+	}
+	dev_info(dev, "alloc nca9555 dev struct ok\n");
+
+	i2c_set_clientdata(i2c, nca9555);
+	nca9555->dev = dev;
+	nca9555->i2c_client = i2c;
+	nca9555->read_reg = nca9555_i2c_read_reg;
+	nca9555->write_reg = nca9555_i2c_write_reg;
+	dev_info(dev, "bind reg ops complete\n");
+
+	if (!dev->of_node) {
+		dev_err(dev, "no device tree node, reject probe\n");
+		return -ENODEV;
+	}
+	u32 dts_addr;
+	ret = of_property_read_u32(dev->of_node, "reg", &dts_addr);
+	if (ret || dts_addr != i2c->addr) {
+		dev_err(dev, "reg mismatch: dts=0x%x client=0x%02x, ret=%d\n",
+			dts_addr, i2c->addr, ret);
+		return -ENODEV;
+	}
+
+	ret = devm_mfd_add_devices(dev, PLATFORM_DEVID_AUTO,
+				    nca9555_devs, ARRAY_SIZE(nca9555_devs),
+				    NULL, 0, NULL);
+	if (ret) {
+		dev_err(dev, "devm_mfd_add_devices failed, ret=%d, err=%pE\n", ret, ERR_PTR(ret));
+		return ret;
+	}
+	dev_info(dev, "probe finished successfully\n");
+
+	return 0;
+}
+
+static const struct i2c_device_id nca9555_i2c_id[] = {
+	{ "nca9555", 0 },
+	{ /* sentinel */ }
+};
+MODULE_DEVICE_TABLE(i2c, nca9555_i2c_id);
+
+static const struct of_device_id nca9555_of_match[] = {
+	{.compatible = "nca9555", },
+	{ /* sentinel */ }
+};
+MODULE_DEVICE_TABLE(of, nca9555_of_match);
+
+static struct i2c_driver nca9555_i2c_driver = {
+	.driver = {
+		   .name = "nca9555",
+		   .of_match_table = of_match_ptr(nca9555_of_match),
+	},
+	.probe = nca9555_i2c_probe,
+	.id_table = nca9555_i2c_id,
+};
+
+static int __init nca9555_i2c_init(void)
+{
+	return i2c_add_driver(&nca9555_i2c_driver);
+}
+/* init early so consumer devices can complete system boot */
+subsys_initcall(nca9555_i2c_init);
+
+static void __exit nca9555_i2c_exit(void)
+{
+	i2c_del_driver(&nca9555_i2c_driver);
+}
+module_exit(nca9555_i2c_exit);
+
+MODULE_DESCRIPTION("NCA9555 core driver");
+MODULE_LICENSE("GPL");
diff --git a/include/dt-bindings/gpio/nca9555-gpio.h b/include/dt-bindings/gpio/nca9555-gpio.h
new file mode 100644
index 000000000..62fead2db
--- /dev/null
+++ b/include/dt-bindings/gpio/nca9555-gpio.h
@@ -0,0 +1,29 @@
+/* SPDX-License-Identifier: GPL-2.0-only */
+/*
+ * GPIO definitions for NCA9555
+ *
+ * Copyright (C) 2023 RPDZKJ
+ */
+
+#ifndef _DT_BINDINGS_NCA9555_GPIO_H
+#define _DT_BINDINGS_NCA9555_GPIO_H
+
+#define IO_00  0
+#define IO_01  1
+#define IO_02  2
+#define IO_03  3
+#define IO_04  4
+#define IO_05  5
+#define IO_06  6
+#define IO_07  7
+
+#define IO_10  8
+#define IO_11  9
+#define IO_12  10
+#define IO_13  11
+#define IO_14  12
+#define IO_15  13
+#define IO_16  14
+#define IO_17  15
+
+#endif /* _DT_BINDINGS_NCA9555_GPIO_H */
diff --git a/include/linux/mfd/nca9555.h b/include/linux/mfd/nca9555.h
new file mode 100644
index 000000000..321aea2bf
--- /dev/null
+++ b/include/linux/mfd/nca9555.h
@@ -0,0 +1,40 @@
+/* SPDX-License-Identifier: GPL-2.0-or-later */
+/*
+ * Analog Devices NCA9555 I/O Expander, keypad controller,
+ * PWM contorller.
+ */
+
+#ifndef __NCA9555_H_
+#define __NCA9555_H_
+
+
+#define GPIO_PORT0 0
+#define GPIO_PORT1 1
+
+#define GPIO_PORT0_INPUT_REG     0x00
+#define GPIO_PORT1_INPUT_REG     0x01
+
+#define GPIO_PORT0_OUTPUT_REG    0x02
+#define GPIO_PORT1_OUTPUT_REG    0x03
+
+#define GPIO_PORT0_POLAR_REG     0x04
+#define GPIO_PORT1_POLAR_REG     0x05
+
+#define GPIO_PORT0_CONFIG_REG    0x06
+#define GPIO_PORT1_CONFIG_REG    0x07
+
+#define NCA9555_ID			0x00
+
+#define NCA9555_BANK(offs)		((offs) >= 8)
+#define NCA9555_BIT(offs)		((offs) >= 8 ? \
+					1u << (offs - 8) : 1u << (offs))
+
+struct nca9555_dev {
+	struct device *dev;
+	struct i2c_client *i2c_client;
+
+	int (*read_reg)(struct nca9555_dev *nca9555, u8 reg, u8 *val);
+	int (*write_reg)(struct nca9555_dev *nca9555, u8 reg, u8 val);
+};
+
+#endif
